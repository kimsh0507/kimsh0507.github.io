---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd" xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% assign visible_posts = site.posts | where_exp: "post", "post.visible == true and post.secret != true and post.hidden != true and post.sitemap != false" %}
  {% for post in visible_posts %}
  <url>
    <loc>{{ post.url | replace: "/index.html", "/" | absolute_url | xml_escape }}</loc>
    <lastmod>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</lastmod>
  </url>
  {% endfor %}

  {% assign public_pages = site.html_pages | where_exp: "page", "page.sitemap != false and page.url != '/404.html'" %}
  {% for page in public_pages %}
  <url>
    <loc>{{ page.url | replace: "/index.html", "/" | absolute_url | xml_escape }}</loc>
    {% if page.last_modified_at %}
    <lastmod>{{ page.last_modified_at | date_to_xmlschema }}</lastmod>
    {% endif %}
  </url>
  {% endfor %}

  {% assign pdf_files = site.static_files | where_exp: "file", "file.extname == '.pdf' and file.sitemap != false" %}
  {% for file in pdf_files %}
  <url>
    <loc>{{ file.path | replace: "/index.html", "/" | absolute_url | xml_escape }}</loc>
    <lastmod>{{ file.modified_time | date_to_xmlschema }}</lastmod>
  </url>
  {% endfor %}
</urlset>
